#include<stdio.h>
#include<stdlib.h>
int main (void)
{
	int i, m[x][2];
	printf("Introduzca el numero de valores que desea guardar (maximo 25)");
	scanf("%d", &x);
	for (i=0;i<x;i++)
	{
		printf("Introduzca el valor a guardar %d",i);
		scanf("%d",&m[i][2]);
	}
}
